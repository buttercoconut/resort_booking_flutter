name: resort_booking_flutter
description: A Flutter frontend for resort booking.
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.0
  http: ^0.13.6
  cached_network_image: ^3.2.3
  google_maps_flutter: ^2.3.0
  intl: ^0.18.0
  # Add other dependencies as needed
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/
